{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "[django:docker] runserver",
            "type": "python",
            "request": "attach",
            "pathMappings": [
                {
                    "localRoot": "${workspaceFolder}",
                    "remoteRoot": "/app"
                }
            ],
            "port": 8001,
            "host": "localhost",
            "redirectOutput": true
            
        }
    ]
}
