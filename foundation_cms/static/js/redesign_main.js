import $ from 'jquery';
import 'foundation-sites';

window.$ = $;
window.jQuery = $;

$(document).foundation();

console.log(
  "😃 If you see this message, the JS watch and build steps are working properly! 😃",
);
