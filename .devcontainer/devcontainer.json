{
	"name": "Mozilla Foundation CMS",
	"dockerComposeFile": ["../docker-compose.yml", "./docker-compose.extend.yml"],
	"context": "..",
	"service": "backend",
	"workspaceFolder": "/app",
	"shutdownAction": "stopCompose",
	"postCreateCommand": "apt-get update && apt-get install -y git && pip install invoke && mkdir -p /root/.ssh && cp -r /root/.ssh-localhost/* /root/.ssh && chmod 700 /root/.ssh && chmod 600 /root/.ssh/*",
	"extensions": [
		"ms-python.python"
	],
	"settings": {
		"terminal.integrated.shell.linux": "bash",
		"python.pipenvPath": "/usr/local/bin/pipenv",
		"python.linting.flake8Path": "flake8"
	}
}