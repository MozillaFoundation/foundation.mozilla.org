//-
  This template is used to generate a base template for usage with Django templates
  `npm build` will compile this to `/network-api/networkapi/templates/pages/base-compiled.html`

extends ../templates/master.pug

block masterParams
  - let isMezzanine = true
  - let pageTitle = `{% block pageTitle %}Mozilla Foundation - {{ page.meta_title }}{% endblock %}`;

block socialMetadata
  | {% block socialMetadata %}{% endblock %}

block memberNotice
  //- Show warning if pages are a Mezzanine draft.
  | {% if page.status == 1 %}<div><strong>Draft preview.</strong> You must save as Published to make it public.</div>{% endif %}

block primaryNav 
  | {% if page.zen_nav == True %}<div id="primary-nav" data-nav-mode="zen"></div>{% else %}<div id="primary-nav"></div>{% endif %}

block heroGuts
  | {% block heroGuts %}{% endblock %}

block content
  | {% block content %}{% endblock %}
