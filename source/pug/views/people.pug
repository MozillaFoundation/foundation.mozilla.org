extends ../templates/master.pug

mixin person(meta)
  .col-lg-3.p-3.person(style=`border:1px solid grey;`)
    h4=meta.name
    p 👤 #{meta.role}
    p ▼ #{meta.location}
    p &hearts;&nbsp;
      each team in meta.teams
        small #{team},&nbsp;

block content
  .container
    .row.p-5
      .col-sm-6
        h1 People
      .col-sm-6
        p Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.

    .row.p-5
      each person, index in data
        +person(data[index])
